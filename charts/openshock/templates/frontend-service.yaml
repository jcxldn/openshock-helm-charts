apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
spec:
  type: ClusterIP
  selector:
    - open.shock.toys/service: frontend
  ports:
    - name: "http"
      port: 80
      targetPort: 80
  internalTrafficPolicy: Local