apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cron
spec:
  type: ClusterIP
  selector:
    - open.shock.toys/service: cron
  ports:
    - name: "http"
      port: 80
      targetPort: 80
  internalTrafficPolicy: Local