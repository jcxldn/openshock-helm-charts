apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openshock.fullname" . }}-test-connection"
  labels:
    {{- include "openshock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openshock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
